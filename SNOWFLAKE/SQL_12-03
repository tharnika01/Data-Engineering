
-- DELETE

SELECT * FROM CUSTOMERS;

-- Delete the customer with CUSTOMER_ID = 101 from the CUSTOMERS table.
DELETE FROM CUSTOMERS 
WHERE CUSTOMER_ID=101;

SELECT * FROM ORDERS;

SELECT * FROM EMPLOYEE_SALARY_HISTORY;
--RENAME
--COLUMN
ALTER TABLE EMPLOYEE_SALARY_HISTORY
RENAME COLUMN EFFECTIVE_DATE TO START_DATE;

--TABLE
ALTER TABLE EMPLOYEE_SALARY_HISTORY
RENAME TO EMP_SALARY_HISTORY;

--RENAME SCHEMA

ALTER SCHEMA SQLPRACTICE
RENAME TO PRACTICE;
SHOW SCHEMAS;
USE SCHEMA PRACTICE;

-- DROP
SELECT * FROM EMP_SALARY_HISTORY;
-- COLUMN
ALTER TABLE EMP_SALARY_HISTORY
DROP COLUMN SALARY;
-- TABLE
DROP TABLE EMP_SALARY_HISTORY;
-- TABLE IF EXISTS
DROP TABLE IF EXISTS EMP_SALARY_HISTORY;

CREATE SCHEMA SCHEMA1;
-- DROP SCHEMA
DROP SCHEMA SCHEMA1;

USE SCHEMA PRACTICE;
--TRUNCATE
TRUNCATE TABLE ORDERS;

SELECT * FROM CUSTOMERS;

ALTER TABLE CUSTOMERS
ADD COLUMN PHONE_NUMBER VARCHAR(15);

ALTER TABLE CUSTOMERS
DROP COLUMN PHONE_NUMBER;

ALTER TABLE CUSTOMERS
ADD COLUMN DISCOUNT DECIMAL(5,2);
SELECT * FROM ORDERS;

ALTER TABLE ORDERS
ADD COLUMN LAST_UPDATED TIMESTAMP DEFAULT CURRENT_TIMESTAMP;

